package it.polimi.ingsw.Client.View.gui.Scene;

public class ConnectionScene {

    /*
    @FXML
    private AnchorPane RootPane;
    @FXML
    private Text ConnectionRequest;
    @FXML
    private TextField ConnectionInsert;
    @FXML
    private Text ConnectionAnswer;
    @FXML
    private Text ConnectionButton;


    @FXML
    public void initialize(){
        ConnectionAnswer.setVisible(false);
        ConnectionButton.addEventHandler(MouseEvent.MOUSE_CLICKED, this:: onConnectionButtonClick);
    }

    private void onConnectionButtonClick(Event event){
        boolean isValidServerPort;
        if(!isValidServerPort){
            ConnectionAnswer.setText("Connection ERROR: try again");
            ConnectionAnswer.setVisible(true)
        }
        else{
            ConnectionAnswer.setText("Connection SUCCESS");
            ConnectionAnswer.setVisible(true);
            scene.changeScene(event, LoginScene.fxml)
        }
    }
     */
}
